node_modules
.next
.git
.gitignore
npm-debug.log
README.md
.env
.env.*
!.env.example
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.prod.yml
*.md
.DS_Store

# NOTE: do NOT ignore the prisma/ directory — the schema and migrations are
# required in the build context for `prisma generate` and `migrate deploy`.
